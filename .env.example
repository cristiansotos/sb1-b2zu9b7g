# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI Configuration (Required for transcription)
# Add this to your Supabase Edge Function secrets
# OPENAI_API_KEY=your_openai_api_key

# App URL Configuration (Required for Edge Functions)
# Add this to your Supabase Edge Function secrets via Dashboard > Edge Functions > Manage secrets
# APP_URL=http://localhost:5173  (for development)
# APP_URL=https://your-domain.com  (for production)
# This is used by the send-family-invitation function to generate invitation links

# Optional: For development
VITE_DEV_MODE=true